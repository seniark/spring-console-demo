spring.application.name=demo-console

# Run without an embedded web server when the "console" profile is active
spring.main.web-application-type=none

console.remote-sync.url=http://localhost:8080/api/sync

